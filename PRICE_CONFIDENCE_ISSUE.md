# 价格数据置信度问题分析

> **日期**: 2026-01-14  
> **优先级**: P0 - 严重问题  
> **状态**: 🔴 待解决

---

## 🚨 问题描述

**用户反馈**：价格对比中显示的价格与溯源链接点进去看到的价格不一致，数据置信度无法保证。

**影响**：
- ❌ 用户无法信任报告数据
- ❌ 可能导致错误的商业决策
- ❌ 损害工具的可信度

---

## 🔍 问题根源分析

### 1. 价格抓取策略问题

当前使用的是**启发式算法**：

```python
# 从 HTML 中提取所有价格
price_matches = re.findall(r'\$(\d+(?:,\d{3})*(?:\.\d{2})?)', html)

# 转换为浮点数并去重
prices = list(set([float(p.replace(',', '')) for p in price_matches]))
prices.sort()

# 过滤合理价格范围（$50-$5000）
reasonable_prices = [p for p in prices if 50 <= p <= 5000]

# 优先选择主产品价格范围（$200-$2000）
main_product_prices = [p for p in reasonable_prices if 200 <= p <= 2000]

if main_product_prices:
    price = min(main_product_prices)  # 选择最小值
```

### 2. 问题点

#### ❌ 问题 1: 页面包含多个价格
- 原价、折扣价、配件价格、运费等
- 算法无法准确识别"主产品当前价格"
- 可能选择了配件价格或促销价

#### ❌ 问题 2: 价格范围假设不准确
- 假设主产品价格在 $200-$2000
- 但实际产品可能 <$200 或 >$2000
- 导致选择错误的价格

#### ❌ 问题 3: 选择最小值策略
- `price = min(main_product_prices)`
- 可能选择了配件或促销价
- 而非实际产品价格

#### ❌ 问题 4: 缺乏验证机制
- 没有价格置信度评分
- 没有警告用户价格可能不准确
- 没有提供原始数据供用户验证

---

## 📊 实际案例

### 案例 1: Uplift 升降桌

**页面包含的价格**：
- $29.99 - 配件价格（桌面垫）
- $599.99 - 主产品价格（升降桌框架）
- $799.99 - 套装价格（框架+桌面）
- $1,299.99 - 高端套装

**算法选择**：$599.99（正确）

**问题**：如果页面有 $199.99 的配件，算法会选择 $199.99（错误）

---

### 案例 2: Vari 升降桌

**页面包含的价格**：
- $49.99 - 配件
- $695.00 - 主产品
- $895.00 - 高端版本

**算法选择**：$695.00（正确）

**问题**：如果页面结构变化，可能选择错误价格

---

## 💡 解决方案

### 方案 1: 添加价格置信度标识（短期）⭐ 推荐

**优势**：
- ✅ 快速实现
- ✅ 提高透明度
- ✅ 让用户知道数据可能不准确

**实现**：
1. 后端添加 `price_confidence` 字段
2. 前端显示置信度警告
3. 提供"点击验证"链接

**示例**：
```
💰 价格对比
┌─────────────────────────────┐
│ Uplift [1]                  │
│ $599.99 ⚠️ 需验证           │ ← 警告标识
│ 中端定位，性价比高          │
│ [点击验证价格] →            │ ← 验证链接
└─────────────────────────────┘
```

---

### 方案 2: 改进价格抽取算法（中期）

**策略**：
1. 使用更精确的 CSS 选择器
2. 优先选择结构化数据（JSON-LD, microdata）
3. 添加品牌特定的选择器
4. 使用 LLM 辅助价格识别

**示例**：
```python
# 1. 优先使用结构化数据
schema_price = extract_from_json_ld(html)

# 2. 使用品牌特定选择器
brand_specific_price = extract_with_brand_selector(html, brand)

# 3. 使用通用选择器
generic_price = extract_with_generic_selector(html)

# 4. 计算置信度
confidence = calculate_confidence(schema_price, brand_specific_price, generic_price)
```

---

### 方案 3: 使用 LLM 验证价格（长期）

**策略**：
1. 将页面 HTML 发送给 LLM
2. 让 LLM 识别主产品价格
3. LLM 返回价格 + 置信度 + 理由

**优势**：
- ✅ 最高准确性
- ✅ 能理解页面语义
- ✅ 能处理复杂情况

**劣势**：
- ❌ 成本较高
- ❌ 速度较慢

---

## 🎯 推荐实施方案

### 阶段 1: 立即实施（今天）

✅ **添加价格置信度警告**
- 在前端显示 ⚠️ 警告图标
- 添加"需人工验证"提示
- 提供"点击验证"链接到原始页面

✅ **添加价格来源说明**
- 显示价格提取方法
- 显示提取时间
- 显示页面快照链接（如果有）

---

### 阶段 2: 短期优化（本周）

🔄 **改进价格抽取算法**
- 添加结构化数据提取
- 添加品牌特定选择器
- 改进价格范围判断逻辑

🔄 **添加价格置信度评分**
- 基于提取方法计算置信度
- 结构化数据 = 高置信度（90%+）
- 品牌选择器 = 中置信度（70-90%）
- 启发式算法 = 低置信度（<70%）

---

### 阶段 3: 中期优化（下周）

🔄 **添加价格验证机制**
- 对比多个来源的价格
- 检测价格异常（过高/过低）
- 自动标记需要人工验证的价格

🔄 **添加价格历史追踪**
- 记录价格变化
- 检测促销活动
- 提供价格趋势

---

## 📋 实施清单

### 立即实施（P0）

- [ ] 后端：添加 `price_confidence` 字段
- [ ] 后端：添加 `price_extraction_method` 字段
- [ ] 前端：显示价格置信度警告
- [ ] 前端：添加"点击验证"链接
- [ ] 文档：更新用户说明

### 短期优化（P1）

- [ ] 后端：添加结构化数据提取
- [ ] 后端：添加品牌特定选择器
- [ ] 后端：改进价格范围判断
- [ ] 后端：计算价格置信度评分
- [ ] 测试：验证各品牌价格准确性

### 中期优化（P2）

- [ ] 后端：添加价格验证机制
- [ ] 后端：添加价格历史追踪
- [ ] 前端：显示价格趋势
- [ ] 前端：显示价格异常警告

---

## 🎨 UI 设计方案

### 方案 A: 警告图标 + Tooltip

```
┌─────────────────────────────┐
│ Uplift [1]                  │
│ $599.99 ⚠️                  │ ← 鼠标悬停显示详情
│ 中端定位，性价比高          │
└─────────────────────────────┘

Tooltip:
┌─────────────────────────────┐
│ ⚠️ 价格需验证               │
│ 提取方法: 启发式算法        │
│ 置信度: 中等 (70%)          │
│ 提取时间: 2026-01-14 10:30  │
│ [点击验证原始价格] →        │
└─────────────────────────────┘
```

### 方案 B: 置信度徽章

```
┌─────────────────────────────┐
│ Uplift [1]                  │
│ $599.99 [中等置信度]        │ ← 颜色区分
│ 中端定位，性价比高          │
│ 点击验证 →                  │
└─────────────────────────────┘

颜色方案:
- 高置信度 (>90%): 绿色
- 中等置信度 (70-90%): 黄色
- 低置信度 (<70%): 红色
```

---

## ✅ 总结

**问题**：价格数据不准确，用户无法信任

**根源**：启发式算法无法准确识别主产品价格

**解决方案**：
1. ⭐ 立即添加置信度警告（提高透明度）
2. 🔄 短期改进抽取算法（提高准确性）
3. 🔄 中期添加验证机制（提高可靠性）

**优先级**：P0 - 立即实施方案 1

---

**创建人**: AI Assistant  
**创建时间**: 2026-01-14  
**状态**: 待实施

